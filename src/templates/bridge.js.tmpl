// The window.{{bridgeName}} members that carry a decision rather than a shape:
// anything reaching for an orivon.* capability, and anything whose answer is
// computed rather than constant. Everything mechanical is declared by name in
// ./members.json and generated -- docs/porting-guide.md step 3.
//
// This file is spliced into the composed bridge, so it is not a module: it
// declares `appMembers`, which the installer calls with the kit
// (kit.refuse, kit.BridgeError, kit.getOrivon, kit.expose).

function appMembers (kit) {
  void kit
  // Every name returned here must appear in members.json's "hand", and every
  // name in "hand" must be returned here. The bridge refuses to install
  // otherwise, before the app's own bundle runs a line.
  return {}
}
